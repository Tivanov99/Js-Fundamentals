function Solve(number) {
    let array = [];
    for (let index = 0; index < number.length; index++) {
        if (index > 0 && index % 2 != 0) {
            array.push(parseInt(number[index])*2);
        }
    }
    console.log(array.reverse().join(' '));
}
